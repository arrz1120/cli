#!/usr/bin/env node
var program=require('commander');

program
  .version('1.1.0')
  .usage('<command> [options]')
  .command('init','构建开发模板 ( Generate develop template )')
  .command('release','复制前端静态资源至 Origin Git ( Copy to origin Git )')
  .parse(process.argv)